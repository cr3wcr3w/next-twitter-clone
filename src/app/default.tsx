// we need default when we have parallel

import Page from '~/app/page';

export default Page;

// when you try to access the modal the page doesnt return only the modal
// export default function Default() {
//     return null
// }

// this file is included because im getting an error after i put parallel with catch all segments in / route
